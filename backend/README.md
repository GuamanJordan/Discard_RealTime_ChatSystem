# Discard_RealTime_ChatSystem
Dependencies
npm install express socket.io mongoose bcrypt jsonwebtoken dotenv cors
npm install -D typescript tsx @types/express @types/socket.io @types/bcrypt @types/jsonwebtoken @types/cors @types/jest jest ts-jest supertest
npm install uuid

